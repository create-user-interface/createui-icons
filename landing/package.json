{
  "name": "createui-icons-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/sync-version.mjs && node scripts/generate-icons-list.mjs",
    "postinstall": "npm run generate",
    "predev": "npm run generate",
    "dev": "astro dev",
    "prebuild": "npm run generate",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.6.0"
  }
}
