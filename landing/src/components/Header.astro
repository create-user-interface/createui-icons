---
import styles from '../styles/Header.module.css';
import type { Lang } from '../i18n';
import { otherLang, pathFor, useT } from '../i18n';

interface Props { lang?: Lang }
const { lang = 'ru' } = Astro.props;
const t = useT(lang);
const other = otherLang(lang);
const switchHref = pathFor(other);
const switchLabel = other.toUpperCase();

const GITHUB_URL = 'https://github.com/create-user-interface/createui-icons';
---

<header class={styles.header}>
  <a href={pathFor(lang)} class={styles.logo}>CreateUI Icons</a>
  <div class={styles.controls}>
    <a
      class={styles.githubLink}
      href={GITHUB_URL}
      target="_blank"
      rel="noopener"
      aria-label={t.header.githubLabel}
    >
      <createui-icon name="git-branch" size="16" stroke="1.5" aria-hidden="true"></createui-icon>
      <span class={styles.githubLabel} data-i18n="header.githubLabel">{t.header.githubLabel}</span>
    </a>
    <a
      id="lang-toggle"
      href={switchHref}
      hreflang={other}
      class={styles.toggleBtn}
      data-lang-switch={other}
      aria-label="Switch language"
    >
      <span class={styles.langLabel} data-lang-label>{switchLabel}</span>
    </a>
    <button id="theme-toggle" class={styles.toggleBtn} aria-label="Switch theme" type="button">
      <span class={styles.themeIcon} aria-hidden="true"></span>
    </button>
  </div>
</header>
