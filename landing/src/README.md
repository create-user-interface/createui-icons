# landing/src/

Исходники лендинга (Astro / HTML / CSS).

Структура страниц, компонентов и стилей. Данные для каталога иконок и changelog подтягиваются из `manifest.json` и метаданных CI при сборке.

После `npm run build` результат попадает в `landing/dist/` и деплоится на сервер.
